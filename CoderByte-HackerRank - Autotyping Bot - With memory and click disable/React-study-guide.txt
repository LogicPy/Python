Sure! Let's go through each of these topics with examples:

### 1. Basic Routing - Matching request paths to their respective components

**Example using React Router:**

First, install React Router:

```bash
npm install react-router-dom
```

**App.js:**

```jsx
import React from 'react';
import { BrowserRouter as Router, Route, Switch } from 'react-router-dom';
import Home from './Home';
import About from './About';

const App = () => (
  <Router>
    <Switch>
      <Route exact path="/" component={Home} />
      <Route path="/about" component={About} />
    </Switch>
  </Router>
);

export default App;
```

**Home.js:**

```jsx
import React from 'react';

const Home = () => (
  <div>
    <h1>Home Page</h1>
  </div>
);

export default Home;
```

**About.js:**

```jsx
import React from 'react';

const About = () => (
  <div>
    <h1>About Page</h1>
  </div>
);

export default About;
```

### 2. Rendering Elements - Generating dynamic HTML elements such as lists or forms

**Example of rendering a list:**

```jsx
import React from 'react';

const List = () => {
  const items = ['Item 1', 'Item 2', 'Item 3'];
  return (
    <ul>
      {items.map((item, index) => (
        <li key={index}>{item}</li>
      ))}
    </ul>
  );
};

export default List;
```

### 3. State Management (Internal Component State) - Creating and updating state values in class components

**Example using class component state:**

```jsx
import React, { Component } from 'react';

class Counter extends Component {
  constructor(props) {
    super(props);
    this.state = { count: 0 };
  }

  increment = () => {
    this.setState({ count: this.state.count + 1 });
  };

  render() {
    return (
      <div>
        <p>Count: {this.state.count}</p>
        <button onClick={this.increment}>Increment</button>
      </div>
    );
  }
}

export default Counter;
```

### 4. Handling Events - Execute functions and update state variables when an event occurs

**Example of handling a button click event:**

```jsx
import React, { useState } from 'react';

const ButtonClick = () => {
  const [message, setMessage] = useState('Hello');

  const handleClick = () => {
    setMessage('Button Clicked');
  };

  return (
    <div>
      <p>{message}</p>
      <button onClick={handleClick}>Click Me</button>
    </div>
  );
};

export default ButtonClick;
```

### 5. ES6 and JavaScript - Creating functions including arrow functions to manipulate data and its display as page elements

**Example of using arrow functions:**

```jsx
import React from 'react';

const ArrowFunctionComponent = () => {
  const greet = () => 'Hello, World!';

  return (
    <div>
      <p>{greet()}</p>
    </div>
  );
};

export default ArrowFunctionComponent;
```

### 6. Form Validation - Validating form data against rules before accepting input and alerting the user with an error message when validation fails

**Example of form validation:**

```jsx
import React, { useState } from 'react';

const FormValidation = () => {
  const [email, setEmail] = useState('');
  const [error, setError] = useState('');

  const handleSubmit = (e) => {
    e.preventDefault();
    if (!email.includes('@')) {
      setError('Invalid email address');
    } else {
      setError('');
      alert('Form submitted');
    }
  };

  return (
    <div>
      <form onSubmit={handleSubmit}>
        <input
          type="email"
          value={email}
          onChange={(e) => setEmail(e.target.value)}
          placeholder="Enter your email"
        />
        <button type="submit">Submit</button>
      </form>
      {error && <p style={{ color: 'red' }}>{error}</p>}
    </div>
  );
};

export default FormValidation;
```

These examples cover basic routing, rendering elements, state management, event handling, ES6 arrow functions, and form validation in React. If you have any further questions or need more detailed examples, feel free to ask!

Sure! Here are examples of the requested concepts:

### 1. Components and Props

#### Controlled Components

**Example using hooks:**

```jsx
import React, { useState } from 'react';

const ControlledComponent = () => {
  const [inputValue, setInputValue] = useState('');

  const handleChange = (e) => {
    setInputValue(e.target.value);
  };

  return (
    <div>
      <input type="text" value={inputValue} onChange={handleChange} />
      <p>Input Value: {inputValue}</p>
    </div>
  );
};

export default ControlledComponent;
```

#### Multiple Components

**Parent and Child Components:**

**ParentComponent.js:**

```jsx
import React from 'react';
import ChildComponent from './ChildComponent';

const ParentComponent = () => {
  const message = 'Hello from Parent';

  return <ChildComponent message={message} />;
};

export default ParentComponent;
```

**ChildComponent.js:**

```jsx
import React from 'react';

const ChildComponent = ({ message }) => {
  return <p>{message}</p>;
};

export default ChildComponent;
```

#### Component Lifecycle

**Example using class components:**

```jsx
import React, { Component } from 'react';

class LifecycleComponent extends Component {
  componentDidMount() {
    console.log('Component mounted');
  }

  componentDidUpdate() {
    console.log('Component updated');
  }

  componentWillUnmount() {
    console.log('Component will unmount');
  }

  render() {
    return <div>Lifecycle Component</div>;
  }
}

export default LifecycleComponent;
```

### 2. Routing with Params

**Example using React Router:**

**App.js:**

```jsx
import React from 'react';
import { BrowserRouter as Router, Route, Switch, Link } from 'react-router-dom';
import Home from './Home';
import User from './User';

const App = () => (
  <Router>
    <nav>
      <Link to="/">Home</Link>
      <Link to="/user/john">User John</Link>
    </nav>
    <Switch>
      <Route exact path="/" component={Home} />
      <Route path="/user/:name" component={User} />
    </Switch>
  </Router>
);

export default App;
```

**User.js:**

```jsx
import React from 'react';
import { useParams } from 'react-router-dom';

const User = () => {
  const { name } = useParams();
  return <div>User: {name}</div>;
};

export default User;
```

### 3. Use APIs

**Example using fetch:**

```jsx
import React, { useEffect, useState } from 'react';

const FetchData = () => {
  const [data, setData] = useState(null);

  useEffect(() => {
    fetch('https://api.example.com/data')
      .then((response) => response.json())
      .then((data) => setData(data));
  }, []);

  return (
    <div>
      {data ? (
        <pre>{JSON.stringify(data, null, 2)}</pre>
      ) : (
        'Loading...'
      )}
    </div>
  );
};

export default FetchData;
```

### 4. Basic Hooks (useState, useEffect)

**Example using `useState` and `useEffect`:**

```jsx
import React, { useState, useEffect } from 'react';

const HooksExample = () => {
  const [count, setCount] = useState(0);

  useEffect(() => {
    console.log('Component mounted or updated');
  }, [count]);

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={() => setCount(count + 1)}>Increment</button>
    </div>
  );
};

export default HooksExample;
```

### 5. Controlled and Uncontrolled Components

#### Controlled Component

```jsx
import React, { useState } from 'react';

const ControlledForm = () => {
  const [value, setValue] = useState('');

  const handleChange = (e) => {
    setValue(e.target.value);
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    alert('Submitted value: ' + value);
  };

  return (
    <form onSubmit={handleSubmit}>
      <input type="text" value={value} onChange={handleChange} />
      <button type="submit">Submit</button>
    </form>
  );
};

export default ControlledForm;
```

#### Uncontrolled Component

```jsx
import React, { useRef } from 'react';

const UncontrolledForm = () => {
  const inputRef = useRef(null);

  const handleSubmit = (e) => {
    e.preventDefault();
    alert('Submitted value: ' + inputRef.current.value);
  };

  return (
    <form onSubmit={handleSubmit}>
      <input type="text" ref={inputRef} />
      <button type="submit">Submit</button>
    </form>
  );
};

export default UncontrolledForm;
```

These examples cover a variety of React concepts, including components and props, routing with params, using APIs, basic hooks, and controlled vs. uncontrolled components. If you need further examples or explanations, feel free to ask!

Sure, let's go through each of these key competencies with examples:

### 1. Business Logic - Integrate framework knowledge with business logic

**Example:**

```jsx
import React, { useState } from 'react';

// Business Logic
const calculateTotalPrice = (items) => {
  return items.reduce((total, item) => total + item.price * item.quantity, 0);
};

// React Component
const ShoppingCart = () => {
  const [cart, setCart] = useState([
    { id: 1, name: 'Apple', price: 0.5, quantity: 3 },
    { id: 2, name: 'Banana', price: 0.2, quantity: 5 },
  ]);

  const totalPrice = calculateTotalPrice(cart);

  return (
    <div>
      <h2>Shopping Cart</h2>
      <ul>
        {cart.map((item) => (
          <li key={item.id}>
            {item.name}: ${item.price} x {item.quantity}
          </li>
        ))}
      </ul>
      <p>Total Price: ${totalPrice.toFixed(2)}</p>
    </div>
  );
};

export default ShoppingCart;
```

### 2. Unit Testing - Creating and running comprehensive unit tests on the React application

**Example using Jest and React Testing Library:**

```jsx
// ShoppingCart.js
import React from 'react';
import { render, screen } from '@testing-library/react';
import ShoppingCart from './ShoppingCart';

test('renders total price correctly', () => {
  render(<ShoppingCart />);
  const totalPriceElement = screen.getByText(/Total Price:/i);
  expect(totalPriceElement).toBeInTheDocument();
});
```

### 3. Debugging React - Identifying problems based on error messages

**Example of common errors and debugging:**

```jsx
import React, { useState } from 'react';

const Counter = () => {
  const [count, setCount] = useState(0);

  const increment = () => {
    setCount(count + 1);
  };

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={increment}>Increment</button>
    </div>
  );
};

export default Counter;
```

Common error: "Too many re-renders."
- **Cause:** Calling `setCount` directly inside the component body or inside an infinite loop.
- **Solution:** Ensure `setCount` is called inside event handlers or useEffect, not directly inside the component body.

### 4. Hooks (useEffect with dependencies, useLayoutEffect, custom hooks)

**Example using `useEffect` with dependencies:**

```jsx
import React, { useState, useEffect } from 'react';

const EffectWithDependencies = () => {
  const [count, setCount] = useState(0);

  useEffect(() => {
    console.log('Effect ran because count changed:', count);
  }, [count]); // Dependency array

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={() => setCount(count + 1)}>Increment</button>
    </div>
  );
};

export default EffectWithDependencies;
```

**Example using `useLayoutEffect`:**

```jsx
import React, { useState, useLayoutEffect, useRef } from 'react';

const LayoutEffectExample = () => {
  const [height, setHeight] = useState(0);
  const divRef = useRef();

  useLayoutEffect(() => {
    setHeight(divRef.current.clientHeight);
  }, []);

  return (
    <div ref={divRef} style={{ height: '100px', backgroundColor: 'lightblue' }}>
      <p>Height of the div: {height}px</p>
    </div>
  );
};

export default LayoutEffectExample;
```

**Example of a custom hook:**

```jsx
import { useState, useEffect } from 'react';

const useFetch = (url) => {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    fetch(url)
      .then((response) => response.json())
      .then((data) => {
        setData(data);
        setLoading(false);
      });
  }, [url]);

  return { data, loading };
};

export default useFetch;

// Usage in a component
import React from 'react';
import useFetch from './useFetch';

const DataFetchingComponent = () => {
  const { data, loading } = useFetch('https://api.example.com/data');

  if (loading) {
    return <div>Loading...</div>;
  }

  return <div>{JSON.stringify(data)}</div>;
};

export default DataFetchingComponent;
```

### 5. Error Boundaries - Catch unexpected errors and display a fallback UI

**Example using error boundaries:**

```jsx
import React, { Component } from 'react';

class ErrorBoundary extends Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false };
  }

  static getDerivedStateFromError(error) {
    return { hasError: true };
  }

  componentDidCatch(error, errorInfo) {
    console.log(error, errorInfo);
  }

  render() {
    if (this.state.hasError) {
      return <h1>Something went wrong.</h1>;
    }

    return this.props.children; 
  }
}

export default ErrorBoundary;

// Usage in a component
import React from 'react';
import ErrorBoundary from './ErrorBoundary';
import BuggyComponent from './BuggyComponent';

const App = () => (
  <ErrorBoundary>
    <BuggyComponent />
  </ErrorBoundary>
);

export default App;
```

### 6. React Context API - Create and use context to pass information to descendants

**Example using React Context API:**

```jsx
import React, { createContext, useState, useContext } from 'react';

const ThemeContext = createContext();

const ThemeProvider = ({ children }) => {
  const [theme, setTheme] = useState('light');

  const toggleTheme = () => {
    setTheme((prevTheme) => (prevTheme === 'light' ? 'dark' : 'light'));
  };

  return (
    <ThemeContext.Provider value={{ theme, toggleTheme }}>
      {children}
    </ThemeContext.Provider>
  );
};

const ThemeButton = () => {
  const { theme, toggleTheme } = useContext(ThemeContext);
  return (
    <button onClick={toggleTheme}>
      Current theme: {theme}
    </button>
  );
};

const App = () => (
  <ThemeProvider>
    <div>
      <h1>Hello, world!</h1>
      <ThemeButton />
    </div>
  </ThemeProvider>
);

export default App;
```

These examples cover a wide range of key React competencies, including integrating business logic, unit testing, debugging, using advanced hooks, implementing error boundaries, and utilizing the React Context API. If you have any further questions or need more detailed explanations, feel free to ask!